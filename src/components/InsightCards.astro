---
import { insightCards } from '@data/consulting';
---

<section class="section-pad">
  <div class="container-narrow">
    <div class="grid gap-8 lg:grid-cols-[0.82fr_1.18fr]">
      <div>
        <p class="eyebrow">我的核心判断</p>
        <h2 class="mt-3 text-3xl font-bold leading-tight text-ink sm:text-4xl">
          很多门店不是缺流量，而是线上表达没有建立信任
        </h2>
        <p class="mt-5 text-base leading-8 text-slate-600">
          本地门店线上获客的问题，往往不只是曝光少。更多时候是顾客已经看到了门店，但没有被说服：不知道你和同行有什么区别，不知道该买哪个团单，不相信页面里的卖点，不知道咨询后能得到什么结果。
        </p>
        <p class="mt-4 text-base leading-8 text-slate-600">
          所以我的工作不是单纯“帮你发内容”，而是先把门店的线上表达、平台结构和转化路径重新梳理。
        </p>
      </div>
      <div class="grid gap-4 sm:grid-cols-2">
        {
          insightCards.map((card, index) => (
            <article class="rounded-lg border border-line bg-white p-5 shadow-sm">
              <p class="text-xs font-semibold text-moss">洞察 {index + 1}</p>
              <h3 class="mt-3 text-xl font-bold leading-snug text-ink">{card.title}</h3>
              <p class="mt-3 text-sm leading-7 text-slate-600">{card.description}</p>
            </article>
          ))
        }
      </div>
    </div>
  </div>
</section>
